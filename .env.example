# Copy to .env in the repository root and fill in. Never commit the real file.
#
# Google Gemini API key. The pipeline reads it from the environment only.
# Free tier is sufficient: the code throttles to stay inside 15 RPM.
GEMINI_API_KEY=
